# SmartCarV2 项目状态与功能清单

更新日期：2026 年 9 月 4 日

## 1 项目概述

SmartCarV2 是基于 Raspberry Pi 4B 和 Yahboom 四轮底盘开发的智能小车控制系统。项目将原厂示例重构为分层、可测试且默认安全停车的控制程序。

系统使用 C 语言处理 GPIO、电机、舵机和传感器等底层操作，使用 Python 实现巡线、避障、停止恢复状态机、远程诊断及可选视觉功能。基础版本由树莓派直接驱动电机；项目同时预留 Arduino 电机后端，两种后端使用相同的 Python 接口。

当前代码已部署至树莓派 `/home/pi/SmartCarV2`。本机和树莓派端的核心测试已经通过，但部分功能仍处于台架验证或等待硬件标定阶段。因此，本文分别使用以下状态：

- **已实现**：代码与接口已经完成。
- **已测试**：已有自动化测试或安全台架测试结果。
- **已实机验证**：已经在当前小车硬件上观察到预期行为。
- **待完成**：缺少实车标定、外部资源或完整验收结果。

## 2 系统架构

```text
摄像头                         巡线与避障传感器
  │                                  │
  ▼                                  ▼
摄像头巡线与视觉检测             C 语言硬件抽象层
  │                                  │
  └────────────► BehaviorManager ◄───┘
                         │
                  停止恢复状态机
                         │
              ┌──────────┴──────────┐
              ▼                     ▼
       树莓派 GPIO 后端        Arduino 串口后端
              │                     │
              └──────────► 电机与舵机
```

行为循环默认以约 10 Hz 运行。安全判断优先于巡线和视觉控制，任何未处理异常、传感器连续失败、通信中断或丢线超时都必须进入停车状态。

## 3 已实现功能

### 3.1 项目结构和运行入口

- 主程序入口 `main.py` 已完成。
- 支持 `--line-source ir|camera` 选择红外或摄像头巡线。
- 支持 `--motor-backend gpio|arduino` 选择电机后端。
- 支持日志等级、串口端口和 TCP 服务参数。
- 配置对象具有启动校验，非法速度、距离阈值和丢线时限会在启动时被拒绝。
- 原副目录中的安全可复用功能已迁移到主目录，主版本统一保留在 `F:\开发板\SmartCarV2`。

### 3.2 C 语言硬件抽象层

`libsmartcar.so` 已提供以下能力：

- GPIO 和 PWM 初始化与清理。
- 四轮电机正转、反转、差速和刹车。
- 三路舵机角度控制。
- RGB 灯输出接口。
- 超声波测距、超时和多次过滤。
- 四路红外巡线输入。
- 两路红外避障输入。
- 车载按钮输入接口。
- 初始化部分失败时释放已申请资源。
- 程序退出时停止 PWM、刹车并释放硬件。

所有初始化失败、未处理异常和程序退出路径都会优先调用刹车接口。

### 3.3 Python 硬件封装与电机后端

- 已使用 `ctypes` 封装 C 语言 HAL。
- `GPIOMotorBackend` 已实现树莓派直接电机控制。
- `ArduinoMotorBackend` 已实现串口发送、短写检测、刹车和关闭处理。
- 行为层只依赖统一的 `run(left, right)` 和 `brake()` 接口，无需了解底层后端。

### 3.4 红外与摄像头巡线

- `IRLineSource` 已实现四路红外状态到横向误差的映射。
- `CameraLineSource` 已实现图像 ROI、灰度阈值、黑线区域和中心误差计算。
- 两种巡线源均返回统一的 `LineReading(error, confidence, detected, timestamp)`。
- PD 差速控制器已实现速度限制和方向修正。
- 已从历史增强版巡线程序迁入误差 EMA 平滑、微分低通和电机速度变化率限制。
- 首次循迹输出和安全恢复后的输出从零速平滑上升；安全刹车不经过速度限制器。
- 丢线处理分为低速保持、按最后方向搜索和最终停车三个阶段。
- 丢线超过停止时限后进入锁定 `FAULT`，不会在下一循环自行恢复运动。

### 3.5 障碍检测与停止恢复

- `ObstacleDetector` 已组合超声波和左右红外避障结果。
- 支持 `CLEAR`、`WARNING`、`BLOCKED` 和 `SENSOR_ERROR` 状态。
- 阻挡或传感器错误会立即刹车。
- 警告距离下使用受限速度。
- 已实现以下安全状态机：

```text
RUNNING --检测到障碍或故障--> PAUSED
PAUSED  --障碍消失---------> WAIT_CLEAR
WAIT_CLEAR --稳定清除并人工确认--> RUNNING
RUNNING --丢线超时---------> FAULT
FAULT   --重新启动和自检----> RUNNING
```

- 实体按钮采用去抖和按下边沿判断。
- 障碍期间一直按住按钮不会造成意外恢复。
- 如果障碍重新出现，已经提交的恢复请求会被取消。
- 摄像头、视觉检测器或巡线源失效时按安全停车处理。

### 3.6 TCP 远程诊断与控制

- TCP 服务默认关闭，必须显式启用。
- 使用共享令牌和恒定时间比较进行身份验证。
- 每条命令以 `#` 结束，支持分片或连续数据帧。
- 同一时间只允许一个客户端连接。
- 接收缓存设有上限。
- `STATUS` 返回行为循环缓存的传感器状态，不会与主循环并发触发超声波测距。
- 已支持以下命令：

```text
AUTH,<token>
STATUS
STOP
RESUME
SPEED,<0-200>
LED,<r>,<g>,<b>
```

`RESUME` 不直接驱动电机。它只提交恢复请求，主行为循环仍会检查系统是否处于 `WAIT_CLEAR`、障碍是否持续清除以及稳定时间是否满足。系统处于 `PAUSED`、`FAULT` 或障碍仍存在时不得恢复。

### 3.7 Arduino 通信后端

树莓派端协议代码和 Arduino 固件已实现：

```text
0xAA | version | command | sequence | length | payload | CRC8
```

- 默认波特率为 115200。
- 支持 `SET_MOTOR`、`BRAKE`、`HEARTBEAT`、`GET_STATUS` 和 `STATUS`。
- CRC8 使用多项式 `0x07`。
- 非法长度、未知命令、错误 CRC 和重复数据不得驱动电机。
- Arduino 看门狗在超过 500 ms 未收到合法电机命令或有效保活条件时自动刹车。
- 心跳不会无限延长旧电机命令的有效时间。

### 3.8 可选视觉功能框架

- 已实现共享 `CameraStream`，摄像头巡线和检测器可以复用同一视频源。
- 已实现 OpenCV DNN 检测线程和异常健康检查。
- 已实现 MobileNet SSD 人形检测结果解码。
- 支持连续多帧确认后停车，降低单帧误报影响。
- 已实现通用 `Detection(class_name, confidence, bbox)` 数据结构。
- 已实现 HSV 颜色目标检测，包括红色的两个色相区间。
- 已实现目标中心到差速电机命令的 `TargetTracker`。
- 已保留小雕像分类接口和 `dress`、`top`、`trousers`、`unknown` 类别设计。

### 3.9 自动化测试和文档

当前共有 31 项自动化测试，覆盖：

- 停止恢复状态机。
- 障碍停车和传感器错误。
- 按钮去抖和长按拒绝。
- TCP 恢复请求的安全门控和取消。
- `FAULT` 锁定。
- 红外巡线误差映射。
- 丢线保持、搜索和停车。
- 误差平滑、微分低通、速度变化率限制和刹车后控制状态复位。
- 配置参数校验。
- Arduino 数据帧、CRC、分片和负载长度。
- 串口短写、刹车和关闭。
- TCP 认证、停止、恢复与速度限制。
- MobileNet SSD 人形结果过滤。
- 视觉目标转向和目标丢失停车。
- 中文网页控制面板的启动命令、参数隔离和非法模式拒绝。

本机和树莓派端均已运行全部 31 项测试，结果全部通过。仓库还包含安装、架构、API、测试计划和开发日志。

## 4 已完成的实机验证

当前树莓派环境：

- 主机名：`yahboom4wd`
- 系统：Raspbian Buster
- Python：3.7.3
- OpenCV：4.1.2
- 项目目录：`/home/pi/SmartCarV2`

已完成以下检查：

- HAL 在树莓派上使用 GCC 和 wiringPi 编译成功。
- 安全 HAL 冒烟测试可以读取四路巡线、两路避障、超声波和按钮状态。
- 测试期间电机保持刹车，没有启动自主行驶循环。
- 超声波距离会随近处障碍物变化，曾获得约 4.5 cm、6.9 cm、11.9 cm、25.1 cm 和 68 cm 等读数；开阔方向读数约为 160 cm 至 198 cm。
- USB 摄像头被识别为 `Sanhao Face`，`/dev/video0` 为图像采集节点，`/dev/video1` 为元数据节点。
- 原厂 `/home/pi/SmartCar/bluetooth_control` 在诊断结束后已恢复为单实例运行。

## 5 未完成或尚未通过实车验收的功能

### 5.1 四路红外巡线标定

四路巡线模块已经从实物照片中定位，位于车头底部并朝向地面。但当前没有黑色胶带或合适的黑白测试材料，因此尚未完成：

- 四个探头的物理顺序确认。
- 黑色和白色表面的有效电平确认。
- 每路电位器或阈值调整。
- 直线、左右弯、交叉线和短时丢线测试。
- PD 参数实车调节。
- 连续 20 分钟红外巡线验收。

### 5.2 左右红外避障标定

实物照片已确认两块亮蓝灯的细长模块是左右红外避障模块。此前监视数据一直为 `[0, 1]`，但采样期间没有分别遮挡两个模块，因此不能据此判断模块损坏。

仍需完成：

- 分别在左右模块前方 5 cm 至 15 cm 放置纸板。
- 验证左右通道是否可以独立变化。
- 调整模块电位器，使无障碍时关闭、近处障碍时触发。
- 记录有效电平并核对软件左右顺序。

### 5.3 摄像头恢复和摄像头巡线验收

USB 摄像头可以被 Linux 和 UVC 驱动识别，但 OpenCV 首帧读取超时，后续底层测试出现 `Device or resource busy`。尚需：

- 重启树莓派或重新插拔摄像头 USB。
- 检查是否存在残留进程占用 `/dev/video0`。
- 使用 V4L2 直接抓取一帧，确认分辨率和像素格式。
- 恢复 OpenCV 持续读取。
- 调整 ROI、阈值、曝光和 PD 参数。
- 完成摄像头断开安全停车测试。
- 连续 20 分钟摄像头巡线验收。

### 5.4 RGB 灯硬件故障

`BST-03 V2.0` 已确认为 RGB 灯板，而不是避障模块。灯板当前始终显示红色，GPIO 直接控制没有产生可见变化。待检查：

- `R/G/B/GND` 插头线序是否与控制板定义一致。
- 接口是否实际提供共阳或共阴驱动。
- 插头、线束和灯板是否损坏。
- 使用独立限流测试确认三个颜色通道。

在硬件问题排除前，软件 `led_set()` 只能视为接口已实现，不能标记为实机功能通过。

### 5.5 实体恢复按钮

实物照片中没有发现可以确认的 `KEY` 或 `START` 按钮。原厂示例还存在 wiringPi 10 同时被不同程序当作按钮和蜂鸣器的硬件版本冲突。

当前已经通过认证 TCP `RESUME` 提供安全替代方案，但以下事项仍未完成：

- 确认当前扩展板是否实际安装实体按钮。
- 如果存在按钮，确认其 GPIO、上下拉和有效电平。
- 如果不存在按钮，决定正式演示使用 TCP 恢复还是增加独立物理按钮。

### 5.6 电机和舵机整车动态验收

电机、舵机和安全关闭代码已经实现，但当前 SmartCarV2 版本尚未完成完整动态验收：

- 四个车轮方向一致性。
- 左右差速方向和最大安全速度。
- 舵机机械限位和中心角度。
- 障碍出现时实际制动距离。
- 丢线超时后实际停车。
- Ctrl+C、进程异常和断电场景。

首次动态测试必须架空轮子并停止原厂控制程序。

### 5.7 人形检测模型和性能测试

人形检测框架和解码器已经实现，但仓库没有包含模型文件。尚未完成：

- 选择具有合法授权的轻量模型。
- 在树莓派上测量推理帧率和 CPU 占用。
- 测试单人、多人、遮挡、不同距离和不同光照。
- 统计漏检率和误报率。
- 完成人形连续多帧确认后的实车停车。

### 5.8 小雕像分类

分类接口和类别设计已经完成，但数据集和模型尚未完成。仍需：

- 采集 `dress`、`top`、`trousers` 和负样本。
- 按雕像主体隔离训练、验证和测试集，避免连续照片跨集合。
- 训练或微调轻量分类器。
- 设置低置信度 `unknown` 阈值。
- 输出准确率、混淆矩阵和树莓派推理速度。

分类结果只用于显示和记录，不直接控制车辆运动。

### 5.9 Arduino 实物联调

协议、Python 后端和 Arduino 固件已经实现，但尚未连接实际 Arduino 和电机驱动板。仍需验证：

- Arduino 引脚与实际电机驱动板匹配。
- 正转、反转、差速和刹车。
- 状态回传。
- 错误 CRC、丢包、重复包和非法长度。
- USB 断开及树莓派进程崩溃后 500 ms 内自动刹车。
- GPIO 后端和 Arduino 后端通过同一套上层行为测试。

## 6 当前硬件识别结果

根据当前实物照片：

- 上层双目外观模块是超声波测距模块。
- 超声波上方的 `BST-03 V2.0` 是 RGB 灯板。
- 下层左右两块亮蓝灯、带透明发射管和黑色接收管的小板是左右红外避障模块。
- 车头底部伸出的横向黑色电路板是四路巡线模块，探头朝向地面。
- 当前照片没有显示可确认的实体启动按钮。

## 7 下一步开发顺序

1. 保持轮子架空，分别遮挡左右红外避障模块并完成灵敏度标定。
2. 使用 10 cm、20 cm、30 cm 和 50 cm 的平面障碍标定超声波阈值。
3. 重启树莓派或重新插拔 USB 摄像头，恢复 `/dev/video0` 首帧采集。
4. 验证 TCP `STOP`、障碍停车、稳定清除和 `RESUME` 的完整闭环。
5. 准备宽度不小于约 16 mm 的黑色胶带或黑纸，完成四路巡线标定。
6. 架空轮子完成四电机方向、差速、舵机限位和异常刹车测试。
7. 在低速条件下完成红外巡线和摄像头巡线场地测试。
8. 两种巡线模式分别连续运行至少 20 分钟，并记录故障、CPU 占用和摄像头帧率。
9. 必做功能全部通过后，再接入人形模型、雕像分类和 Arduino 实物。

## 8 安全注意事项

- 原厂 `bluetooth_control` 与 SmartCarV2 不能同时运行，两者会争用相同的 GPIO 和 PWM。
- 第一次电机测试必须架空四个轮子，并在旁边保留可直接切断电源的方式。
- 不要通过网络命令绕过障碍、传感器故障或稳定清除时间。
- 摄像头断开、传感器连续失败、串口中断和未处理异常都必须停车。
- wiringPi 10 在不同原厂示例中分别被定义为按钮和蜂鸣器，确认硬件版本前不要驱动该引脚作为蜂鸣器。
- RGB 灯线序未确认前不要反复尝试高占空比输出。

## 9 仓库主要文件

```text
SmartCarV2/
├── main.py                         主程序入口
├── smartcar/                       Python 行为与设备封装
│   ├── behavior.py                 安全状态机
│   ├── line_source.py              红外和摄像头巡线
│   ├── obstacle.py                 超声波和红外避障
│   ├── motor_backend.py            GPIO 与 Arduino 后端
│   ├── protocol.py                 Arduino 二进制协议
│   ├── remote.py                   TCP 诊断与控制
│   ├── vision.py                   摄像头和 DNN
│   └── target_tracker.py           视觉目标转向
├── hal/                            C 语言硬件抽象层
├── arduino/SmartCarMotorController Arduino 固件
├── tools/sensor_monitor.py         安全只读传感器监视
├── tests/                          自动化测试
├── docs/                           安装 架构 API 和测试文档
└── DEVELOPMENT_LOG.md              开发与实机调试记录
```

## 10 当前结论

项目的软件主体、安全状态机、两种巡线接口、避障逻辑、TCP 诊断、Arduino 协议及可选视觉框架已经建立，自动化测试全部通过。项目尚不能标记为整车完成，因为四路巡线、左右避障、摄像头、RGB 灯、电机动态行为、可选模型和 Arduino 实物仍缺少完整实机验收。

当前应把开发重点放在传感器标定、摄像头恢复和低速整车安全测试。必做功能全部通过后，再开展人形检测、雕像分类和 Arduino 实物扩展。
